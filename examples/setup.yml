schemas: 
 - _roles_backend
 - _roles_userlogin
 - domain_reseller
 - email 
 - jabber 
 - web

schema_dirs:
 - ../schemas/
 - /etc/carnivora/

role_prefix: carnivora_

pre_code: |
 -- UUIDs
 CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
 -- Enable Python 3 as function language
 CREATE EXTENSION IF NOT EXISTS "plpython3u";


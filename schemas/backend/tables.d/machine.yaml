name: machine
description: |
 Physical or virtual machines that hosts services.

primary_key:
 - name

templates:
 - commons.options

columns:
 -
  name: name
  type: dns.t_domain
  description: Machine name

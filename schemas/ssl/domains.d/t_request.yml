name: t_request
type: bytea
description: DER cert
checks:
 -
  name: cert
  description: really a cert?
  check: |
    VALUE IS NULL OR
    array_length((ssl.request_info(VALUE))."subjectAltName", 1) > 0

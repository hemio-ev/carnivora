name: t_cert_request
type: bytea
description: DER cert
checks:
 -
  name: cert
  description: really a cert?
  check: |
    VALUE IS NULL OR
    array_length((ssl.cert_request_info(VALUE))."subjectAltName", 1) > 0

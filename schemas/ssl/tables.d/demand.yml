name: demand
description: |
 Service needs certificates

templates:
 - system.reference_service_entity

primary_key:
 - id

columns:
 - name: id
   type: uuid
   description: UUID
   default: commons._uuid()
 - name: acme_ca
   type: dns.t_domain
   description: ACME Server
   null: true
   references: ssl.acme_ca.name


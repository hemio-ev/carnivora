name: demand_domain
description: |
  Domains for which the cert should be issued

  .. todo:: check domain is subdomain of registered

primary_key:
 - demand_id
 - domain

columns:
 - name: demand_id
   type: uuid
   description: Certificate demand id
   references: ssl.demand.id
 - name: domain
   type: dns.t_hostname
   description: Domain
 - name: registered
   type: dns.t_hostname
   description: registered domain
   references: dns.registered.domain


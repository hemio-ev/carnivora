name: name_host
description: Name server entries based on host name server entry suggestions from system.host_name
templates:
 - dns.name
inherits:
 - dns.name

primary_key:
 - name
 - type

foreign_key:
 -
  name: rr_by_host
  columns:
   - type
   - host_name
  ref_table: system.host_name
  ref_columns:
   - type
   - name

columns:
 -
  tag: column
  name: host_name
  type: dns.domain_name
  description: Predefined entry for host 


name: name_host
description: Name server entries based on host name server entry suggestions from system.host_name
templates:
 - dns.name
inherits:
 - dns.name

primary_key:
 - name
 - type

columns:
 -
  tag: column
  name: host_name_entry
  type: uuid
  references: system.host_name.id
  description: Predefined entry for host 


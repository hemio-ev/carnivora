name: raw
description: Direct name server entries. Not based on host name server entry suggestions from system.host_name

primary_key:
 - domain
 - type

columns:
 -
  name: domain
  type: dns.domain_name
  description: domain of entry 
 -
  template: commons.uuid_serial
 -
  template: dns.type
 -
  template: dns.rdata
 -
  template: dns.ttl


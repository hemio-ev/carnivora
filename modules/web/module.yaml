name: web
description: |
 Web
priv_all_all:
 - system
priv_usage:
 - userlogin

domains:
 -
  name: t_cert
  type: text
  description: PEM cert
  checks:
   -
    name: base64
    description: no newlines in db
    check: VALUE ~ '^[a-zA-Z\d/+]+[=]{0,1}$'

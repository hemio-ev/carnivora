name: service_dns
description: Resource records for services

primary_key:
 - id

templates:
 - system.reference_service

columns:
 -
  template: commons.uuid_serial
 -
  template: dns.type
 -
  template: dns.rdata
 -
  template: dns.ttl

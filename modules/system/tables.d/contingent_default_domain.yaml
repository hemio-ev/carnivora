name: contingent_default_domain
description: |
 Number of times the service can be used by a user per domain.
 The exact interpretation depends on the module.

templates:
 - system.reference_service
 - system.contingent

primary_key:
 - service
 - domain

columns:
 -
  name: domain
  type: dns.t_domain
  description: Domain for which the contingent holds

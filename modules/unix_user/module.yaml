name: unix_user 
description: Explicit passwd entriesfor shell acounts, sftp etc.
dependencies: [ hosts ]

column_templates:
 -
    template: user_id
    name: user_id
    type: SERIAL
    description: Unix user identifier

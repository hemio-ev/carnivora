name: address
description: "Collection of all addresses known to the mailservers to avoid collisions. No direct functionalities follows from entries in this list. Many tables have references to this table."

primary_key:
 - local_part
 - domain

columns:
 -
  tag: column_tpl
  template: email.local_part
 -
  tag: column_tpl
  template: email.domain
 -
  tag: column_tpl
  template: user.owner

#checks:
# -
#  name: address_local_valid
#  check: "true" #address_local_new_valid(address)
#  description: what a check


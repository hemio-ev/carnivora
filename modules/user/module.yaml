name: user
description: Users that can own things

column_templates:
 -
  template: owner
  name: owner
  type: varchar(50)
  description: for ownage
  references: user.user.name

roles:
 -
  name: userlogin
  description: Do user actions via this group
  login: false

function_templates:
 -
  template: userlogin
  description: complicated...
  priv_execute: 
   - userlogin

  variables:
   -
    name: v_owner
    type: varchar

  body_prelude: |
   -- begin userlogin prelude
   v_owner := (SELECT "name" FROM "user".get_login());
   -- end userlogin prelude

